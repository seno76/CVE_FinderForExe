# GETTING_STARTED.md

# Начало работы с Bochka

## Быстрая настройка (2 минуты)

### 1. Установка

```bash
# Клонируем проект
git clone https://github.com/yourusername/Bochka.git
cd Bochka

# Устанавливаем зависимости
pip install -r requirements.txt
```

### 2. Первый запуск

```bash
python apps/interactive_scanner.py
```

Появится интерактивное меню:

```
═══════════════════════════════════════════════
    💾 СКАНЕР УЯЗВИМОСТЕЙ БДУ ФСТЕК
═══════════════════════════════════════════════

1. 📁 Сканировать папку
2. 📂 Сканировать несколько папок
3. 📋 Сканировать по реестру (все программы)
4. 💾 Сканировать все диски (реестр + диски)
0. ❌ Выход

Выберите опцию: 
```

### 3. Выбор режима сканирования

**Вариант 1: Быстрое сканирование реестра**

```
Выберите опцию: 3
```

Это получит ВСЕ установленные на компьютере программы (~150-200) и найдёт уязвимости автоматически.

**Вариант 2: Сканирование конкретной папки**

```
Выберите опцию: 1
Введите путь папки: C:\Program Files
```

**Вариант 3: Все диски одновременно**

```
Выберите опцию: 4
```

Отсканирует реестр + все найденные Program Files на всех дисках.

## Результаты

После завершения сканирования вам будут предложены два отчёта:

### 📄 JSON отчёт

Автоматически сохранится как `report.json`:

```json
{
  "metadata": {
    "total_files_scanned": 157,
    "files_with_vulnerabilities": 2,
    "total_vulnerabilities": 3
  },
  "findings": [
    {
      "software_name": "Docker Desktop",
      "software_version": "4.44.3",
      "vulnerabilities": [...]
    }
  ]
}
```

### 🌐 HTML отчёт

Открыть можно в любом браузере:

```
report.html
```

Содержит:
- Графики по уровню опасности
- Таблица всех найденных уязвимостей
- Способы проверки и рекомендации
- Цветовое кодирование по критичности

## Что дальше?

### Регулярные проверки

Запускайте сканирование еженедельно, чтобы выявлять новые уязвимости при обновлении БДУ:

```bash
python apps/interactive_scanner.py
```

### Автоматизация

Добавьте регулярное выполнение через Task Scheduler (Windows):

```batch
# scan.bat
cd C:\Bochka
python apps/interactive_scanner.py
```

### Интеграция

Используйте JSON отчёт в вашей системе мониторинга безопасности:

```python
import json
import subprocess

# Запустить сканирование
subprocess.run(['python', 'apps/cli_scanner.py', 
                '--folder', 'C:\\Program Files',
                '--json-output', 'report.json'])

# Загрузить результаты
with open('report.json') as f:
    results = json.load(f)

# Обработать
vulnerable = results['findings']
for item in vulnerable:
    print(f"{item['software_name']}: {len(item['vulnerabilities'])} уязвимостей")
```

## Решение проблем

### "Ошибка: не найдена БДУ"

Это нормально при первом запуске. Приложение:
1. Загружает БДУ с интернета (~30 сек)
2. Сохраняет в кеш
3. Использует кеш для быстрых проверок (~1 сек)

### "Доступ запрещён к реестру"

Запустите PowerShell с правами администратора:

```powershell
Right-click → Run as administrator
```

Потом запустите:

```bash
python apps/interactive_scanner.py
```

### Сканирование идёт слишком долго

1. Сканирование папки может занять время зависит от количества .exe файлов
2. Используйте режим реестра (опция 3) для быстрого получения установленного ПО
3. Или укажите конкретную папку вместо всего диска

## Примеры команд

### Сканировать только Program Files

```bash
python apps/interactive_scanner.py
# Выберите опцию 1, введите: C:\Program Files
```

### Сканировать несколько папок

```bash
python apps/interactive_scanner.py
# Выберите опцию 2
# Введите пути через запятую: C:\Program Files, D:\Software
```

### Получить JSON отчёт для обработки

```bash
python apps/cli_scanner.py --folder "C:\Program Files" --json-output findings.json
```

Потом в Python:

```python
import json
with open('findings.json') as f:
    data = json.load(f)
    
print(f"Всего уязвимостей найдено: {data['metadata']['total_vulnerabilities']}")
print(f"Критических: {data['metadata']['critical_vulnerabilities']}")
```

## Документация

- [README.md](README.md) — полный обзор проекта
- [CONTRIBUTING.md](CONTRIBUTING.md) — как помочь с разработкой
- Исходный код — хорошо задокументирован с примерами

Удачи с анализом безопасности! 🔒
